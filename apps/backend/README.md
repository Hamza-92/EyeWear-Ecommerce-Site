# Backend and admin application

The Laravel REST backend and Inertia/React admin. Run the API with `npm run dev:backend` and the
Vite admin assets with `npm run dev:admin` from the repository root. Backend conventions live in
`../../docs/backend.md`; admin conventions live in `../../docs/admin.md`.
