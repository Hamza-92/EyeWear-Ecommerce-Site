# Unit tests

Place isolated PHP unit tests in this directory. Keeping the directory under version control also
ensures PHPUnit's configured `Unit` test suite exists on case-sensitive CI runners before the first
unit test is added.
