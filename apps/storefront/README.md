# Storefront application

The customer-facing Next.js App Router application. Run it from the repository root with
`npm run dev:storefront`. Architecture and rendering rules live in `../../docs/storefront.md`.
